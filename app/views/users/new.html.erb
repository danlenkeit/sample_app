<% provide(:title, "SIGNUP") %>
<h1>YA WANNA SIGN UP?</h1>

<div class="row">
	<div class="col-md-6 col md-offset-3">
		<%= form_for(@user) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "CREATE", class: "btn btn-primary" %>
		<% end %>
	</div>
</div>

